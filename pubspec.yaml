name: task_manager
description: A Flutter-based CRUD application using Back4App for task management.
publish_to: 'none' # Remove this line if you intend to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # The core package for connecting to Back4App (Parse Server)
  parse_server_sdk_flutter: ^8.0.0
  
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
# Final attempt to force Zapp package refresh